#include <stdio.h>
#include <math.h>
#include "parts.h"

float dot_product(Vec2 *v1, Vec2 *v2){
    return ((v1->x * v2->x) + (v1->y * v2->y));
}
Vec2 add_vectors(Vec2 *v1, Vec2 *v2){
    return 
}
Vec2 subtract_vectors(Vec2 *v1, Vec2 *v2){
    return 
}
float vector_magnitude(Vec2 *v1, Vec2 *v2){
    return 
}